import { Grid } from "@chakra-ui/react";
import TodoCard from "components/ui/TodoCard";
import React from "react";

const PerDayWork = () => {
  return (
    <Grid>
      <TodoCard />
    </Grid>
  );
};

export default PerDayWork;
